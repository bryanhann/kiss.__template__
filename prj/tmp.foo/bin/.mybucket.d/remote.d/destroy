#!/usr/bin/env bash
$__init__
. header $*

$my_name .mybucket remote exists || exit
clr_bold "$ aws s3 rm $my_bucket_remote --recursive"
            aws s3 rm $my_bucket_remote --recursive
clr_bold "aws s3 rb $my_bucket_remote"
          aws s3 rb $my_bucket_remote
$my_name .mybucket remote exists

