#!/usr/bin/env bash
$__init__
name=$(basename ${BASH_SOURCE[1]})
a="$1"
b="$2"
shift 2
[ "$a" == "$b" ]
err=$?
[   $err == 0 ] && . _pass $name "(assert [$a]==[$b])"
[ ! $err == 0 ] && . _fail $name "(assert [$a]==[$b])"

return $err
