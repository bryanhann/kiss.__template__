#!/usr/bin/env bash
$__init__
. header $*
if [ ! "${my_repo:0:3}" == "tmp" ]; then
    echo not a tmp repo. Not deleting
    exit
fi

clr_bold "deleting remote repo"
$my_name .gh danger destroy $my_repo --force
$my_name .myrepo remote exists
